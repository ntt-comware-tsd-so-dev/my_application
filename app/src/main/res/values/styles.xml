<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.Base">
    </style>

    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/light_green_500</item>
        <item name="colorPrimaryDark">@color/light_green_900</item>
        <item name="colorAccent">@color/light_green_A400</item>
    </style>

    <!-- Style for text views in list items that show the device name -->
    <style name="DeviceListItemTextView" parent="@android:style/TextAppearance.Medium">
        <item name="android:gravity">center_vertical</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_gravity">center_vertical</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_margin">10dp</item>
    </style>

    <!-- Style for the device status, shown to the right of list view items -->
    <style name="DeviceListItemStatusTextView" parent="@android:style/TextAppearance.Medium">
        <item name="android:textStyle">italic</item>
        <item name="android:gravity">center_vertical|right</item>
        <item name="android:layout_gravity">center_vertical</item>
        <item name="android:layout_margin">10dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
    </style>

    <!-- Style for text views used as a title -->
    <style name="PageHeader" parent="@android:style/TextAppearance.Large">
        <item name="android:layout_gravity">center_horizontal</item>
        <item name="android:background">@color/light_green_700</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">5dp</item>
        <item name="android:paddingTop">20dp</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!-- Style for a full-height dialog -->
    <style name="FullHeightDialog" parent="android:style/Theme.Dialog">
        <item name="android:windowNoTitle">true</item>
    </style>

</resources>
